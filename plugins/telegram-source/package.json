{
  "name": "@curatedotfun/telegram-source",
  "version": "0.0.1",
  "description": "Telegram source plugin for curate.fun",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "type": "module",
  "peerDependencies": {
    "every-plugin": "^0.1.0",
    "typescript": "^5.0.0"
  },
  "devDependencies": {
    "@effect/language-service": "^0.41.1",
    "@effect/vitest": "^0.26.0",
    "@rspack/cli": "^1.5.2",
    "@rspack/core": "^1.5.2",
    "@types/node": "^24.6.0",
    "dotenv": "^17.2.2",
    "every-plugin": "link:every-plugin",
    "msw": "^2.3.0",
    "vitest": "^3.2.4",
    "zephyr-rspack-plugin": "^0.0.59"
  },
  "scripts": {
    "build": "rspack build && tsc -p tsconfig.build.json",
    "dev": "rspack serve",
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage"
  },
  "dependencies": {
    "telegraf": "^4.16.3"
  }
}
