{
	"name": "@examples/curatedotfun",
	"private": true,
	"scripts": {
		"start": "bun run main.ts",
		"dev": "bun run --watch main.ts",
		"worker": "bun run workers/submission.worker.ts",
		"worker:dev": "bun run --watch workers/submission.worker.ts",
		"inspect": "bun run inspect-db.ts",
		"db:generate": "drizzle-kit generate",
		"db:migrate": "drizzle-kit migrate",
		"db:studio": "drizzle-kit studio",
		"embeddings:migrate": "bun run scripts/generate-embeddings.ts"
	},
	"devDependencies": {
		"@effect/language-service": "^0.41.1",
		"@types/bun": "^1.2.21",
		"drizzle-kit": "^0.31.4",
		"telegraf": "^4.16.3"
	},
	"peerDependencies": {
		"typescript": "^5"
	},
	"dependencies": {
		"@ai-sdk/provider": "^2.0.0",
		"@ai-sdk/provider-utils": "^3.0.10",
		"@libsql/client": "^0.15.15",
		"@orpc/server": "^1.8.9",
		"@xenova/transformers": "^2.17.2",
		"ai": "^5.0.59",
		"drizzle-orm": "^0.44.5",
		"every-plugin": "0.1.6",
		"fast-check": "^4.3.0",
		"hono": "^4.9.8"
	}
}
