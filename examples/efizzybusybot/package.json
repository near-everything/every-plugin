{
	"name": "@examples/curatedotfun",
	"private": true,
	"scripts": {
		"start": "bun run main.ts",
		"dev": "bun run --watch main.ts",
		"worker": "bun run workers/submission.worker.ts",
		"worker:dev": "bun run --watch workers/submission.worker.ts",
		"inspect": "bun run inspect-db.ts",
		"db:generate": "drizzle-kit generate",
		"db:migrate": "drizzle-kit migrate",
		"db:studio": "drizzle-kit studio"
	},
	"devDependencies": {
		"@types/bun": "^1.2.21",
		"better-sqlite3": "^12.2.0",
		"drizzle-kit": "^0.31.4",
		"telegraf": "^4.16.3"
	},
	"peerDependencies": {
		"typescript": "^5"
	},
	"dependencies": {
		"@orpc/server": "^1.8.9",
		"drizzle-orm": "^0.44.5",
		"effect": "^3.17.14",
		"every-plugin": "link:every-plugin",
		"hono": "^4.9.8"
	}
}
